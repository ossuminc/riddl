domain foo {
  user U is "an example user"
  context App is {
    entity Account is { ??? }
  }
  epic TestCase is {
    user U wants to "hmm" so that "haw"
    case one is {
      user U wants to "hmm" so that "haw"
      step entity foo.App.Account refuses user U "not authorized"
    }
  }
}
