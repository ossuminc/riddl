context other by author Reid is { ??? }
context context by author Reid is {
  adaptor adaptor from context other is { ??? }
  term term is briefly "not interesting"
  function function is { ??? }
  event event is { at: TimeStamp }
  handler handler is {
    on event event {
      | error "This is an error"
    }
  }
  repository repository is { ??? }
  connector pipe is { ??? }
  source source is { ??? }
  sink sink is { ??? }
  flow flow is { ??? }
  merge merge is { ??? }
  split split is { ??? }
  saga saga is {
   step a is {
     |nothing
   } reverted by {
     |nothing
   }
   step b is {
     |nothing
   } reverted by {
     |nothing
   }
 }
}
