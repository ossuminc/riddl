domain foo {
  user U is "an example user"
  application App is { ??? }
  context Gateway is { ??? }
  command DoSomething is { ??? }
  epic TestCase is {
    case one is {
      step send command DoSomething from application App to context Gateway
    }
  }
}
