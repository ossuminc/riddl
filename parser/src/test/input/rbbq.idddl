// #everything
// #domains
domain rbbq.operation.restaurant {
// #domains
  device context Kitchen {
        // TBD
  }
  context Order {
  }
  context Menu {}
  context
  context Reservations {
    entity Reservation = combine { partyName: String, phoneNumber: String,
     location: Id, time: Time, date: Date
     } consumes channel 'foo' produces channel 'bar'
  }
}
// #everything
