context Order is {
  entity Order is {
    option is aggregate
    state OrderState is {
      orderId is OrderId,
      customerId is CustomerId
    }
    handler foo  is { ??? }
  }
} explained as {
| This bounded context defines the handling of restaurant orders.
}
